# Copilot Instructions

- Keep answers grounded in project architecture (`docs/architecture.md`).
- Preserve API contracts in `src/redmine_rag/api/schemas.py`.
- Add tests for endpoint behavior changes.
- Do not add dependencies without updating `README.md` and `pyproject.toml`.
